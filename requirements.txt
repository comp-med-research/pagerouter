# Core data
pandas>=2.0
numpy>=1.26

# Machine learning routers (Experiment 4)
scikit-learn>=1.4
xgboost>=2.0

# Clustering (Experiment 2)
scipy>=1.12

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Anthropic API (Experiment 5)
anthropic>=0.25

# Multi-provider VLM agents (Experiment 6 ablation)
openai>=1.30
google-generativeai>=0.8

# Image handling (agentic router base64 encoding)
Pillow>=10.0

# PyTorch / embeddings pipeline (MLP router)
torch>=2.0
torchvision>=0.15
transformers>=4.51
tqdm>=4.66

# Visual diagnostics / KNN (optional)
open-clip-torch>=2.24
faiss-cpu>=1.7
umap-learn>=0.5

# Testing
pytest>=8.0
